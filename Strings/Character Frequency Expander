#include <stdio.h>
#include<string.h>
#include<ctype.h>
int main(){
    char str[100];
    fgets(str,sizeof(str),stdin);
    int len=strlen(str);
    for(int i=0;str[i]!='\0';i++){
        if(isdigit(str[i])){
            for(int j=0;j<str[i]-'0';j++)
            printf("%c",str[i-1]);}}}
