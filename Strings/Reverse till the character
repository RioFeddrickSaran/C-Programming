#include <stdio.h>
#include <string.h>
int main() {
    char str[100],c;
    scanf("%s %c",str,&c);
    int len = strlen(str);
    for(int j=0;str[j]!='\0';j++) {
        if(str[j]==c){
    for (int i=0;i<=j/2;i++) {
        char temp = str[i];
        str[i] = str[j-i];
        str[j-i] =temp;}}}
    printf("%s",str);}
