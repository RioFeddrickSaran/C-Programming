#include <stdio.h>
#include <string.h>
int main(){
    char str[100],c;
    scanf("%s %c",str,&c);
    for(int i=0;str[i]!='\0';i++){
        if(str[i]==c){
            break;} 
        else{
            printf("%c",str[i]);}}}
